<section class="thirdPartyLogin">
  <aside class="arrow">Sign in (or sign up) with an account you already have</aside>
  <%= link_to "Login with Facebook", "/auth/facebook", class: "fbLink" %>
  <%= link_to "Login with Twitter", "/auth/twitter", class: "twitterLink" %>
</section>

<% if flash[:email_required] %>
 <div id='emailDialog' class='hide'>
 <p>Please enter your <span class='highlight'>email address</span> to finish creating your account</p>
 <%= form_tag sessions_provide_email_path, method: :post, remote: true %>
    <%= hidden_field 'userid', flash[:user_id] %>
    <%= hidden_field 'provider', flash[:provider] %>
    <%= label_tag 'email', 'Email', for: 'user[email]', class: 'screenReaderOnly' %>
    <%= email_field 'user', 'email' %>
	  <%= submit_tag 'Sign in', disable_with: 'Signing you in...' %>
    <small><a href=''>Your email is used solely for login purposes</a></small>
  </form>
  </div>
<% end %>