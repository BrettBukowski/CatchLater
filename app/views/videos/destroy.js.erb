$("#<%= @video.id %>").slideUp("slow");
/* Adding a post-animation callback somehow causes the the animation
  to stutter... */
  setTimeout(function() {
    $("#<%= @video.id %>").remove();
  }, 1000);