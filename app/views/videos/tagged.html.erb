<%= render partial: "heading", locals: { 
			heading: @tag,
} %>

<%= render partial: "nav" %>

<% if notice %>
  <div id="notice"><%= notice %></div>
<% end %>

<section class="queue videoList">
	<%= render @videos %>
</section>
